# VB_MASTER_KEY is the only required deployment secret.
# Generate with: openssl rand -base64 32
VB_MASTER_KEY=

# Process and database bootstrap (application settings are configured in UI).
VB_DATABASE_URL=file:./data/veerabahu.db
VB_PORT=3000
